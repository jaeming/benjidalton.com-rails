.row
  %h2.section_title Blog
  .border &nbsp;
  .columns.medium-8
    .left.new-post
      - if current_user
        = link_to 'New Article', new_article_path, class: 'button tiny round'
    = render @articles
  .columns.medium-4.side-badge
    %h2 Archives
    - @article_months.sort.reverse.each do |month, articles|
      %br
      %small.archives
        = h month.strftime("%B %Y")
      - for article in articles
        %br
        %small.archive-post
          =h link_to article.title, article_path(article), onclick: "routeTo(this)"
